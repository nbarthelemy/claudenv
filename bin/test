#!/bin/bash
# Claudenv Test Runner
# Usage: bin/test [test-file]

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"

exec bash "$REPO_ROOT/tests/run-tests.sh" "$@"
